in Star component we have sent 'no' and 'callbackFunction' (setRating) which
needs to be call at the time of click star. 

function Star({no, rating}){ 
    
  . . .  onClick = { () => { rating(no+1) } }  . . . 
}


star-rating-step2

we can pass handler function also which has body what needs to be done on click

<Star rating = { () => { setRating(no);}}

function Star({no, clickHandler}){ 
    
  . . .  onClick = {clickHandler}  . . . 
}

-------------------
star-rating-step3
displaying solid stars on clck


we have passed a type of star at the time of click, state changes and 
and parent component re-rendered, because of that all child components are re-rendered
at the time of re-rendering we passed type based on the state, 
Component get that state and returned the star based on the type.

