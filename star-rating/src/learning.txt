in Star component we have sent 'no' and 'callbackFunction' (setRating) which
needs to be call at the time of click star. 

function Star({no, rating}){ 
    
  . . .  onClick = { () => { rating(no+1) } }  . . . 
}


star-rating-step2

we can pass handler function also which has body what needs to be done on click

<Star rating = { () => { setRating(no);}}

function Star({no, clickHandler}){ 
    
  . . .  onClick = {clickHandler}  . . . 
}

